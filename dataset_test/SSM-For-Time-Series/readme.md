model_based_portfolio.py in tst directory has the SSQP based portfolio optimization method for maximizing Sharpe ratio.

In the same tst directory, ssm.py has the S4 state space model.

run_8layer.py runs trains and evaluates the S4 model with both sentiments and without sentiments.

debug_8layer.py was written for debugging run_8layer.py and it can be ignored for now.

